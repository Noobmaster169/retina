# dependencies
node_modules/

# next.js
.next/
out/

# env files — the templates are documentation, not secrets
.env*
!.env.example

# typescript / build
*.tsbuildinfo
next-env.d.ts

# misc
.DS_Store
*.pem
*.log

docs/superpowers/

# Windows download metadata next to every file in emails/
*:Zone.Identifier
# python (proxy/)
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Playwright MCP output
.playwright-mcp/

# eval harness output
backend/eval/reports/

# setuptools writes these on every `pip install -e proxy`. Tracked, they made
# the clone dirty on the box, and auto-deploy.sh refuses to touch a dirty tree.
*.egg-info/
proxy/uv.lock
